<h1><%= @sub.title %></h1>
<h3>Description</h3>

<p><%= @sub.description%></p>

<h3>Posts</h3>
<ul>
    <% @posts.each do |post| %>
        <li>
            <a href="<%= post_url(post) %>"><%= post.title %></a>
        </li>
    <% end %>
</ul>

<a href="<%= new_post_url %>">Make New Post!</a>
<br>
<a href="<%= edit_sub_url(@sub)%>">Edit</a>